---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  labels:
    name: "alert-manager-monitor1"
  name: "alert-manager-monitor1"
  namespace: "default"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "alert-manager-monitor1"
  strategy:
    type: "RollingUpdate"
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  minReadySeconds: 5
  revisionHistoryLimit: 10
  template:
    metadata:
      labels:
        name: "alert-manager-monitor1"
        app: "alert-manager-monitor1"
        podConflictName: "alert-manager-monitor1"
      annotations:
        serviceInstanceName: "monitor1"
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                name: "alert-manager-monitor1"
                podConflictName: "alert-manager-monitor1"
            namespaces:
            - "default"
            topologyKey: "kubernetes.io/hostname"
      hostPID: false
      hostNetwork: true
      containers:
      - args:
        - "/opt/edp/monitor1/conf/alertmanager-bootstrap.sh"
        image: "registry.cn-hangzhou.aliyuncs.com/udh/monitor:1.0.0"
        imagePullPolicy: "Always"
        readinessProbe:
          exec:
            command:
            - "/bin/bash"
            - "-c"
            - "curl --fail --connect-timeout 15 --max-time 15 \"http://`hostname`:9093/\"\
            \n"
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        name: "alert-manager-monitor1"
        resources:
          requests: {}
          limits: {}
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: "/opt/edp/monitor1/data"
          name: "data"
        - mountPath: "/opt/edp/monitor1/log"
          name: "log"
        - mountPath: "/etc/localtime"
          name: "timezone"
        - mountPath: "/opt/edp/monitor1/conf"
          name: "conf"

      nodeSelector:
        alert-manager-monitor1: "true"
      terminationGracePeriodSeconds: 30
      volumes:
      - hostPath:
          path: "/opt/edp/monitor1/data"
        name: "data"
      - hostPath:
          path: "/opt/edp/monitor1/log"
        name: "log"
      - hostPath:
          path: "/etc/localtime"
        name: "timezone"
      - hostPath:
          path: "/opt/edp/monitor1/conf"
        name: "conf"

